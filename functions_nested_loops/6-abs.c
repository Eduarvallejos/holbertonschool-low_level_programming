#include "main.h"
#include <stdlib.h>

/**
 * _abs - Return the absolute value of a number.
 * @n: Receive a number.
 *
 * Return: The absolute value of the input number.
 */
int _abs(int n)
{
return (abs(n));
}
