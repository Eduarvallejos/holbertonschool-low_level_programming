#include "main.h"
#include <stdio.h>
/**
 * mul - Multiply integers.
 * @a: First integer,
 * @b: Whole second.
 * Return: will return the result of the multiplication of a and b.
 */
int mul(int a, int b)
{
return (a * b);
}
