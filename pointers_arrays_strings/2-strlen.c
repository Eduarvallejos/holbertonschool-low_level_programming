#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - Print in stdout the len of string.
 * @s: Value a Read len.
 *
 * Return: the len of string.
 */
int _strlen(char *s)
{
int len = 0;
while
	(s[++len] != 0);

return (len);
}
