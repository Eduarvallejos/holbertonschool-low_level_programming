#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - Print in stdout the len of string.
 * @s: Value a Read len.
 *
 * Return: the len of string.
 */
int _strlen(char *s)
{
int contador = 0;
while
	(s[++contador] != '\0');

return (contador);
}
